source "https://rubygems.org"
gemspec

# Address Faraday's retry middleware warning by explicitly
# including the faraday-retry gem. This removes the runtime
# message when building the site.
gem "faraday-retry"